// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Cambiar al dominio final cuando se defina (lamarquessa.co, etc.)
  site: 'https://lamarquessa.co',
});
